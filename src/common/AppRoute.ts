export enum AppRoute {
  Home = "/",
  Catalog = "/catalog",
  FlowerDetails = "/catalog/:flowerId",
  About = "/about",
  Contact = "/contact",
  Checkout = "/checkout",
  CheckoutConfirm = "/checkout/confirm",
}
